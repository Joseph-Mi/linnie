# Site-specific build configuration
# These settings are applied via KAS to the build environment

# Build optimizations - adjust based on your system
# Run "nproc" to get available CPUs
PARALLEL_MAKE = "-j ${@os.cpu_count()}"
BB_NUMBER_THREADS = "${@os.cpu_count()}"

# Shared cache locations (set via devcontainer environment)
# DL_DIR and SSTATE_DIR are configured in devcontainer.json
